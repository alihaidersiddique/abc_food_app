import 'package:flutter/material.dart';

class ExampleModel extends StatelessWidget {
  const ExampleModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
